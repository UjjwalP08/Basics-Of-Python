# n = input("Enter  your name:-")

# if n.isnumeric():
#     raise Exception("No is not allowed")

# suppose here after that code our 1000 line code written which take 1hour for execution
# so if our input is wrong so our whole program is execute and take a time,so avoid that
# mistakes we are use exception in python as above example you show if our input is wrong
# so our program is not able to execute

# print(f"your name is {n}")

c = input("Enter your name:-")
try:
    print(a)

except Exception as e:

    if c =="nothing":
        raise ValueError("Harry is blocked he is not allowed")

    print("Exception handled")
    

